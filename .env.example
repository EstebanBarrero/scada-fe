# Backend API base URL (no trailing slash)
# In dev, Vite's proxy handles /api → http://localhost:8000
# Override this only when the backend is on a different host/port
VITE_API_BASE_URL=http://localhost:8000
